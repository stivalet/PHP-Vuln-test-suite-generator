<?xml version="1.0"?> //no use
<execQuery>
  <sample>
    <comment>execQuery : normal query</comment>
    <code>$conn = mysql_connect('localhost', 'mysql_user', 'mysql_password'); // Connection to the database (address, user, password)
  $res = mysql_query($query); //execution
  mysql_close($conn);</code>
    <isPrepared prepared = "0"/>
    <relevancy>1</relevancy>
  </sample>

  <sample>
    <comment>execQuery : prepared query</comment>
    <code>$conn = mysql_connect('localhost', 'mysql_user', 'mysql_password'); // Connection to the database (address, user, password)
  $stmt = $conn->prepare($query);
  $stmt->bind_param("i", $checked_data)
  $stmt->execute()
  mysql_close($conn);</code>
    <isPrepared prepared = "1"/>
    <relevancy>1</relevancy>
  </sample>
</execQuery>
