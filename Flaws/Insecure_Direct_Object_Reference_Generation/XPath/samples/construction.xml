<?xml version="1.0"?>
<construction>
  <sample>
    <path>
      <dir>concatenation</dir>
    </path>
    <comment>construction : concatenation</comment>
    <code>$query="//Course[@id=". $checked_data . "]";
$xml = simplexml_load_file("users.xml");
//flaw
$res=$xml->xpath($query);</code>
    <safety safe = "0"/>
    <constrant />
    <relevancy>1</relevancy>
  </sample>

  <sample>
    <path>
      <dir>concatenation</dir>
      <dir>right_verification</dir>
    </path>
    <comment>construction : concatenation and checks if the user is allowed to see this data</comment>
    <code>$query="//Course[@id=". $checked_data . "and @allowed=". $_SESSION[userid] . "]";
$xml = simplexml_load_file("users.xml");
//flaw
$res=$xml->xpath($query);</code>
    <safety safe = "1"/>
    <constraint />
    <relevancy>1</relevancy>
  </sample>
</construction>
